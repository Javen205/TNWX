{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "tsc": "lerna run tsc --stream",
    "tsc:watch": "lerna run watch --stream",
    "publish": "lerna publish --access public"
  },
  "devDependencies": {
    "lerna": "^3.17.0",
    "prettier": "^1.18.2",
    "typescript": "^3.6.4"
  }
}
